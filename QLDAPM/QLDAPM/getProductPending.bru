meta {
  name: getProductPending
  type: http
  seq: 12
}

get {
  url: http://localhost:5050/api/products-pending
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

auth:bearer {
  token: 
}

tests {
  const response = res.getBody()
  bru.setEnvVar('accessToken', response.token)
}
