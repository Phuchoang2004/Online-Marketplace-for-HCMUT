meta {
  name: Get Pending Product
  type: http
  seq: 12
}

get {
  url: http://localhost:5000/api/products-pending
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

tests {
  const response = res.getBody()
  bru.setEnvVar('accessToken', response.token)
}
